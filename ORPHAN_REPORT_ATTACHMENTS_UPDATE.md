# تحديث تقرير بيانات اليتيم - إضافة الوثائق من قاعدة البيانات

## التاريخ
29 يناير 2026

## الملخص
تم تحديث نظام توليد تقارير بيانات الأيتام (PDF) لجلب جميع الوثائق والمرفقات بشكل حقيقي من جدول `attachments` في قاعدة البيانات، بدلاً من استخدام بيانات وهمية.

## التغييرات الرئيسية

### 1. تحديث `GenerateOrphanReportPdf.php`

#### إضافة استيراد نموذج Attachment
```php
use App\Models\Attachment;
```

#### دالة `collectReportData()` - إضافة جلب الوثائق
- جلب جميع الوثائق المرتبطة بالملف من جدول `attachments`
- فصل الصور الشخصية عن باقي الوثائق
- إضافة صورة المكفول (`orphan_photo`)
- إضافة صورة المعيل (`guardian_photo`)
- إضافة جميع الوثائق الأخرى في `attachments`

#### دالة `getAllAttachments()` - جديدة
تقوم بـ:
1. جمع جميع أرقام الهوية المتعلقة بالملف:
   - رقم هوية اليتيم/المكفول
   - رقم هوية المعيل
   - أرقام هوية أفراد الأسرة من جدول `re_people`
   - أرقام الهوية من جدول `data`

2. جلب جميع الوثائق لهذه الأرقام من جدول `attachments`

3. تنظيم الوثائق مع المعلومات التالية:
   - اسم الشخص ونوعه (مكفول/معيل/فرد من الأسرة)
   - نوع الوثيقة
   - مسار الملف
   - التحقق من وجود الملف فعلياً
   - تحديد نوع الملف (صورة أم PDF)

#### دالة `getPersonNameByIdentity()` - جديدة
- الحصول على اسم الشخص من رقم الهوية
- البحث في جداول: `sponsorships`, `re_people`, `data`

#### دالة `getDocumentTypeName()` - جديدة
- الحصول على اسم نوع الوثيقة من جدول `document_types`
- دعم البحث بالرقم أو النص

#### دالة `getFamilyMembers()` - تحديث
- إضافة جلب الصورة الشخصية لكل فرد من أفراد الأسرة
- إرجاع معلومات الصورة مع بيانات الفرد

### 2. تحديث قالب `orphan-report.blade.php`

#### إضافة الصور الشخصية في الأماكن المخصصة

##### صورة المكفول
- عرض الصورة الشخصية للمكفول بجانب معلوماته الأساسية
- إضافة عمود للصورة في الجدول الأول (rowspan=2)
- حجم الصورة: 100x120 بكسل

##### صورة المعيل
- عرض الصورة الشخصية للمعيل في قسمه الخاص
- إضافة عمود للصورة في جدول المعيل (rowspan=2)
- حجم الصورة: 100x120 بكسل

##### صور أفراد الأسرة
- إضافة عمود "الصورة الشخصية" في جدول أفراد الأسرة
- عرض صورة كل فرد بجانب بياناته
- حجم الصورة: 80x90 بكسل
- عرض "لا توجد صورة" إذا لم تكن موجودة

#### صفحة الوثائق والمرفقات (جديدة)
- إضافة صفحة جديدة بعد البيانات الأساسية
- تنظيم الوثائق حسب الشخص
- عرض الوثائق في شبكة 3×3 (ثلاث وثائق في كل صف)
- كل وثيقة تحتوي على:
  - عنوان نوع الوثيقة
  - معاينة الصورة (للصور)
  - أيقونة PDF (لملفات PDF)
  - اسم الملف
  - رسالة "الملف غير موجود" إذا كان الملف مفقوداً

## آلية العمل

### 1. جلب الوثائق
```php
// جمع أرقام الهوية
$identityNumbers = [
    $sponsorship->identity_number,          // اليتيم
    $sponsorship->guardian_identity_number, // المعيل
    // أفراد الأسرة من re_people
    // أفراد من جدول data
];

// جلب الوثائق
$dbAttachments = Attachment::whereIn('person_identity_number', $identityNumbers)
    ->orderBy('person_identity_number')
    ->orderBy('file_type')
    ->get();
```

### 2. التمييز بين الصور الشخصية والوثائق الأخرى
```php
$isPersonalPhoto = in_array($attachment->file_type, [
    'صورة شخصية', 
    'الصورة الشخصية', 
    'personal_photo', 
    '1', 
    1, 
    'صوره شخصيه'
]);

// التحقق من اسم الملف أيضاً
if (!$isPersonalPhoto && $attachment->stored_file_name) {
    $fileName = strtolower($attachment->stored_file_name);
    $isPersonalPhoto = str_contains($fileName, 'personal') || 
                      str_contains($fileName, 'صورة') || 
                      str_contains($fileName, 'صوره');
}
```

### 3. معالجة مسارات الملفات
```php
// تحويل المسار النسبي إلى مسار مطلق
if (!str_starts_with($filePath, '/') && !str_starts_with($filePath, 'http')) {
    if (str_starts_with($filePath, 'storage/')) {
        $filePath = storage_path('app/public/' . str_replace('storage/', '', $filePath));
    } else {
        $filePath = public_path($filePath);
    }
}

// التحقق من وجود الملف
$fileExists = file_exists($filePath);
```

## بنية جدول attachments المطلوبة

```sql
attachments:
  - id
  - person_identity_number  (رقم الهوية)
  - stored_file_name        (اسم الملف المخزن)
  - file_path               (مسار الملف)
  - file_type               (نوع الوثيقة)
  - file_size
  - created_at
  - updated_at
```

## ملاحظات هامة

1. **رقم الهوية في اسم الملف**: يتم استخدام `person_identity_number` للربط بين الوثيقة والشخص المعني

2. **أنواع الوثائق المدعومة**:
   - صور شخصية (jpg, jpeg, png, gif)
   - ملفات PDF
   - أي نوع ملف آخر (يعرض كأيقونة)

3. **معالجة الملفات المفقودة**: النظام يتحقق من وجود الملف ويعرض رسالة مناسبة إذا كان مفقوداً

4. **التنظيم**: 
   - الصور الشخصية تظهر في الأماكن المخصصة (بجانب الاسم)
   - باقي الوثائق تظهر في صفحة منفصلة منظمة حسب الشخص

5. **الأداء**: 
   - يتم جلب جميع الوثائق في استعلام واحد
   - استخدام eager loading لتقليل عدد الاستعلامات

## الاستخدام

```php
// تشغيل Job لتوليد التقرير
GenerateOrphanReportPdf::dispatch($sponsorshipId);

// أو بشكل متزامن
$job = new GenerateOrphanReportPdf($sponsorshipId);
$job->handle();
```

## النتيجة النهائية

التقرير الآن يحتوي على:
1. **الصفحة الأولى**: بيانات اليتيم والمعيل والأسرة مع الصور الشخصية
2. **الصفحة الثانية وما بعدها**: جميع الوثائق المرفقة منظمة حسب الشخص

## الملفات المعدلة

1. `app/Jobs/GenerateOrphanReportPdf.php`
   - إضافة دوال جديدة لجلب الوثائق
   - تحديث دالة collectReportData
   - تحديث دالة getFamilyMembers

2. `resources/views/user/dashboard/pdf/orphan-report.blade.php`
   - إضافة عرض الصور الشخصية في الجداول
   - إضافة صفحة جديدة للوثائق

## التحسينات المستقبلية

1. إضافة فلترة للوثائق حسب النوع
2. إضافة ترتيب مخصص للوثائق
3. دعم عرض الوثائق في صفحات منفصلة إذا كان عددها كبير
4. إضافة watermark للصور في التقرير
5. دعم ضغط الصور لتقليل حجم PDF
