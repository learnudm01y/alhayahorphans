# ملخص سريع: توليد رقم الملف الداخلي عند استيراد الكفالات

## 📌 الفكرة الأساسية

عند رفع ملف Excel للكفالات، يتم تحديد رقم الملف الداخلي بناءً على موقع الشخص في قاعدة البيانات:

---

## ✅ الحالات الأربع

### 1. موجود في `data` ➜ استخدام رقم الملف الموجود
```
رقم الهوية موجود في: data.data_id_number
الإجراء: استخدام data.file_id_number الموجود
مثال: هوية 123456789 → رقم ملف 001234 (موجود مسبقاً)
```

### 2. موجود في `re_people` ➜ توليد رقم ملف جديد
```
رقم الهوية موجود في: re_people.re_id_number
الإجراء: توليد file_id_number جديد باستخدام generateUniqueReservedCode()
مثال: هوية 987654321 → رقم ملف 005678 (تم توليده)
```

### 3. موجود في `dead_people` ➜ توليد رقم ملف جديد
```
رقم الهوية موجود في: dead_people.dead_id_number
الإجراء: توليد file_id_number جديد باستخدام generateUniqueReservedCode()
مثال: هوية 456789123 → رقم ملف 007890 (تم توليده)
```

### 4. غير موجود في أي جدول ➜ رفض الصف
```
رقم الهوية غير موجود في أي جدول
الإجراء: رفض الصف وإضافته لقائمة الأخطاء
```

---

## 🔄 ترتيب البحث

```
1️⃣ البحث في data.data_id_number
   ↓ (إذا لم يُعثر عليه)
2️⃣ البحث في re_people.person_id
   ↓ (إذا لم يُعثر عليه)
3️⃣ البحث في dead_people.father_id أو dead_people.mother_id
   ↓ (إذا لم يُعثر عليه)
❌ رفض الصف
```

---

## ⚠️ ملاحظات هامة عن الأعمدة

### جدول `re_people`
- العمود المستخدم: **`person_id`** (رقم الهوية الشخصية)
- ليس `re_id_number`

### جدول `dead_people`
- العمودان المستخدمان: **`father_id`** و **`mother_id`**
- ليس `dead_id_number`
- يتم البحث في كلا العمودين لأن الملف قد يحتوي على رقم هوية الأب أو الأم المتوفى

---

## 💾 حفظ البيانات

### جدول `sponsorships`
```sql
internal_file_number = رقم الملف (المولد أو الموجود)
```

### جدول `guardian_bank_accounts`
```sql
guardian_registration = رقم الملف (نفس الرقم)
```

---

## 📝 مثال عملي

### Excel يحتوي على 3 أشخاص:

| هوية المعيل | الموقع في قاعدة البيانات | رقم الملف | الإجراء |
|-------------|--------------------------|-----------|---------|
| 123456789   | data                     | 001234    | ✅ استخدام الموجود |
| 987654321   | re_people                | 005678    | 🆕 توليد جديد |
| 456789123   | dead_people              | 007890    | 🆕 توليد جديد |
| 999999999   | غير موجود                | -         | ❌ رفض |

---

## 🔧 الدالة المستخدمة

```php
generateUniqueReservedCode('data', 'file_id_number')
```

- توليد رقم فريد من 6 أرقام (000001 → 999999)
- التحقق من عدم وجود تكرار
- استخدام Transaction للأمان

---

## 📊 الإحصائيات

بعد الاستيراد، سيظهر:
- ✅ عدد الكفالات المضافة بنجاح
- 🆕 عدد أرقام الملفات المولدة
- 🔄 عدد أرقام الملفات المستخدمة
- ❌ عدد الأخطاء

---

## ⚠️ ملاحظات هامة

1. **لا يتم نقل الأشخاص**: الشخص يبقى في جدوله الأصلي (re_people أو dead_people)
2. **رقم الملف للكفالة فقط**: يُستخدم في جدول sponsorships فقط
3. **عدم التكرار مضمون**: الدالة تتحقق من عدم وجود تكرار
4. **Transaction آمن**: جميع العمليات ضمن Transaction واحد

---

**الموقع:** `app/Http/Controllers/Admin/SponsorshipController.php` → دالة `import()`  
**التاريخ:** 8 ديسمبر 2025  
**الحالة:** ✅ مطبق ومفعّل
