# دليل إصلاح مشكلة عرض بيانات Excel على الاستضافة المشتركة

## المشكلة
البيانات المستوردة من Excel لا تظهر في جدول إدارة السجلات على الاستضافة المشتركة.

## السبب
اختلاف في أرقام ID لحالات الطلبات بين البيئة المحلية والاستضافة.

## الحل المطبق
تم تطوير نظام ديناميكي للبحث عن حالة "مقبول" تلقائياً مع معالجة شاملة للأخطاء.

## خطوات النشر على الاستضافة

### 1. رفع الملفات المحدثة
ارفع هذه الملفات إلى الاستضافة:
- `app/Services/ExcelImportService.php` (الملف الرئيسي المحدث)
- `hosting_diagnosis.php` (سكريپت التشخيص)
- `hosting_fix.php` (سكريپت الإصلاح)

### 2. تشغيل سكريپت التشخيص
```bash
php hosting_diagnosis.php
```
هذا السكريپت سيفحص:
- جدول `request_status` والحالات المتاحة
- السجلات المستوردة من Excel وحالاتها
- محاكاة استعلام DataTable
- تحديد السبب الدقيق للمشكلة

### 3. تشغيل سكريپت الإصلاح
```bash
php hosting_fix.php
```
هذا السكريپت سيقوم بـ:
- تحديد ID حالة "مقبول" الصحيح
- إصلاح جميع السجلات المستوردة من Excel
- تعيين الحالة الصحيحة لجميع السجلات

### 4. اختبار الاستيراد الجديد
- ارفع ملف Excel جديد
- تأكد من ظهور البيانات في الجدول
- راقب ملفات السجل (logs) للتأكد من عمل النظام الديناميكي

## الميزات الجديدة

### 1. البحث الديناميكي المحسن
- بحث مباشر عن "مقبول"
- بحث مع تجاهل حالة الأحرف
- بحث جزئي للعثور على حالات مشابهة
- استخدام القيم الافتراضية حسب البيئة

### 2. معالجة شاملة للأخطاء
- تسجيل مفصل لجميع العمليات
- خطط احتياطية متعددة
- تعامل مع فشل الاتصال بقاعدة البيانات

### 3. تحديد البيئة التلقائي
- البيئة المحلية: الافتراضي = 2
- بيئة الإنتاج: الافتراضي = 4

## ملفات السجل (Logs)
راقب هذه الرسائل في ملفات السجل:

```
Excel import: Found accepted status - نجح العثور على الحالة
Excel import: Using fallback status - استخدام حالة بديلة
Excel import: No status found - لم يتم العثور على أي حالة
```

## استكشاف الأخطاء

### المشكلة: لا تزال البيانات لا تظهر
**الحل:**
1. تشغيل `hosting_diagnosis.php` مرة أخرى
2. فحص ملفات السجل
3. التأكد من أن جدول `request_status` يحتوي على حالة "مقبول"

### المشكلة: خطأ في تشغيل السكريپتات
**الحل:**
1. التأكد من صلاحيات الملفات
2. فحص إعدادات PHP على الاستضافة
3. التأكد من اتصال قاعدة البيانات

### المشكلة: حالة خاطئة مُعيّنة
**الحل:**
1. فحص جدول `request_status` يدوياً
2. تحديث الحالة يدوياً إذا لزم الأمر
3. إعادة تشغيل سكريپت الإصلاح

## الاختبار النهائي
1. استيراد ملف Excel جديد
2. التحقق من ظهور البيانات في `/admin/records-management`
3. فحص حالة السجلات في قاعدة البيانات
4. مراجعة ملفات السجل للتأكد من عدم وجود أخطاء

## الدعم
إذا استمرت المشكلة:
1. انسخ نتائج `hosting_diagnosis.php`
2. انسخ رسائل الخطأ من ملفات السجل
3. أرسل التفاصيل للدعم الفني

---

**ملاحظة:** هذا الإصلاح يعمل على جميع أنواع الاستضافة ويتكيف تلقائياً مع اختلافات قواعد البيانات.
