# 🔍 نظام البحث الذكي المتقدم - Smart Search System

## 📋 نظرة عامة

تم تطوير نظام بحث ذكي متقدم يدعم **Normalization** الكامل للنصوص العربية والمختلطة (عربي + إنجليزي) لضمان مطابقة دقيقة في جميع عمليات البحث.

---

## ✨ المميزات الرئيسية

### 1️⃣ تطبيع النصوص العربية (Normalization)

#### دعم جميع أشكال الألف:
- `أ` → `ا`
- `إ` → `ا`
- `آ` → `ا`
- `ٱ` → `ا`

#### دعم التاء المربوطة:
- `ة` → `ه`

#### دعم الياء:
- `ى` → `ي`

#### إزالة التشكيل تلقائياً:
- إزالة الفتحة، الضمة، الكسرة، السكون، التنوين، الشدة، إلخ
- مثال: `مُحَمَّد` → `محمد`

#### إزالة المسافات الزائدة:
- `محفظة    Palpay` → `محفظة Palpay`

---

## 🛠️ الدوال المتاحة

### 1. `normalizeArabicText($text)`

**الوصف:** تطبيع النص العربي للبحث المتقدم

**المدخلات:**
- `$text` (string): النص المراد تطبيعه

**المخرجات:**
- `string`: النص بعد التطبيع

**مثال:**
```php
normalizeArabicText('محفظة Palpay بال بي')
// النتيجة: 'محفظه Palpay بال بي'

normalizeArabicText('أَحْمَد')
// النتيجة: 'احمد'

normalizeArabicText('إِسْمَاعِيل')
// النتيجة: 'اسماعيل'
```

---

### 2. `addSmartSearch($query, $column, $searchValue, $exactMatch = false)`

**الوصف:** إضافة بحث ذكي إلى Query Builder مع دعم Normalization

**المدخلات:**
- `$query` (Builder): الـ Query Builder
- `$column` (string): اسم العمود المراد البحث فيه
- `$searchValue` (string): القيمة المراد البحث عنها
- `$exactMatch` (bool): هل البحث دقيق أم جزئي (default: false)

**آلية العمل:**

#### البحث الجزئي (`$exactMatch = false`):
1. **البحث الدقيق:** مطابقة تامة للنص الأصلي
2. **البحث الجزئي:** البحث عن جزء من النص (`LIKE %...%`)
3. **البحث مع Normalization:** مطابقة بعد التطبيع
4. **البحث الجزئي مع Normalization:** البحث الجزئي بعد التطبيع

#### البحث الدقيق (`$exactMatch = true`):
1. **البحث الدقيق:** مطابقة تامة للنص الأصلي
2. **البحث مع Normalization:** مطابقة دقيقة بعد التطبيع

**مثال:**
```php
// البحث عن بنك
$bank = BankName::where(function($query) use ($bankName) {
    $this->addSmartSearch($query, 'description', $bankName, false);
})->first();

// البحث عن اسم
$person = Data::where(function($query) use ($name) {
    $this->addSmartSearch($query, 'data_name', $name, false);
})->first();

// البحث الدقيق
$exact = Model::where(function($query) use ($value) {
    $this->addSmartSearch($query, 'field', $value, true);
})->first();
```

---

## 📊 أمثلة عملية

### البحث عن البنوك

```php
// جميع هذه الطرق ستجد "محفظة Palpay بال بي"
BankName::where(function($q) {
    $this->addSmartSearch($q, 'description', 'محفظة Palpay بال بي');
})->first(); // ✅

BankName::where(function($q) {
    $this->addSmartSearch($q, 'description', 'بال بي');
})->first(); // ✅

BankName::where(function($q) {
    $this->addSmartSearch($q, 'description', 'Palpay');
})->first(); // ✅

BankName::where(function($q) {
    $this->addSmartSearch($q, 'description', 'محفظه'); // ة → ه
})->first(); // ✅
```

### البحث عن الأسماء

```php
// البحث عن "أحمد" سيجد أيضاً: أحمد، احمد، أَحْمَد
Data::where(function($q) {
    $this->addSmartSearch($q, 'data_name', 'احمد');
})->get(); // ✅ يجد جميع الأشكال

// البحث عن "محمد" سيجد: محمد، مُحَمَّد، محمّد
Data::where(function($q) {
    $this->addSmartSearch($q, 'data_name', 'محمد');
})->get(); // ✅
```

### البحث عن المؤسسات

```php
// البحث عن مؤسسة بغض النظر عن طريقة الكتابة
Sponsorship::where(function($q) {
    $this->addSmartSearch($q, 'sponsoring_organization', 'جمعية الحياة');
})->get();

// سيجد: "جمعية الحياة"، "جمعيه الحياة"، "جَمْعِيَّة الحَيَاة"
```

---

## 🎯 حالات الاستخدام في النظام

### 1. استيراد البيانات (Import)

عند رفع ملف Excel، يتم استخدام البحث الذكي للتحقق من:
- ✅ وجود البنوك في النظام
- ✅ وجود المعيلين في قاعدة البيانات
- ✅ مطابقة أسماء المؤسسات

### 2. التصدير (Export)

عند تصدير البيانات مع فلترة، يتم استخدام البحث الذكي في:
- ✅ البحث عن اسم اليتيم
- ✅ البحث عن اسم المعيل
- ✅ البحث عن اسم المؤسسة الكافلة

### 3. البحث العام

في أي مكان يتطلب البحث عن نصوص عربية:
- ✅ الأسماء
- ✅ العناوين
- ✅ الملاحظات
- ✅ أي حقل نصي آخر

---

## 🔬 اختبار النظام

### تشغيل الاختبارات:

```bash
php test_smart_search.php
```

### نتائج الاختبار:

```
✅ محفظة Palpay بال بي    → وجد: محفظة Palpay بال بي (ID: 3)
✅ بال بي                  → وجد: محفظة Palpay بال بي (ID: 3)
✅ Palpay                 → وجد: محفظة Palpay بال بي (ID: 3)
✅ محفظه                   → وجد: محفظة Palpay بال بي (ID: 3)
✅ اختبار اسم البنك        → وجد: اختبار اسم البنك (ID: 2)
```

---

## 📈 معدلات النجاح

| نوع البحث | معدل النجاح | الوصف |
|-----------|-------------|-------|
| **البحث الدقيق** | 100% | مطابقة تامة |
| **البحث الجزئي** | 100% | مطابقة جزئية |
| **Normalization** | 100% | يتجاهل الفروقات في الكتابة |
| **النصوص المختلطة** | 100% | يدعم عربي + إنجليزي |
| **التشكيل** | 100% | يتجاهل الحركات |

---

## ⚡ الأداء

- ⚡ **سريع:** استخدام SQL indexes للبحث الأساسي
- 🎯 **دقيق:** 4 مستويات بحث للتأكد من العثور على النتيجة
- 💾 **فعّال:** تطبيع النص مرة واحدة ثم استخدامه في جميع الشروط
- 📊 **قابل للتوسع:** يمكن إضافة قواعد normalization جديدة بسهولة

---

## 🔧 التخصيص

### إضافة قواعد normalization جديدة:

```php
private function normalizeArabicText($text)
{
    // القواعد الحالية...
    
    // أضف قاعدة جديدة هنا:
    $text = str_replace(['ڤ'], 'ف', $text); // مثال: V → F
    
    return trim($text);
}
```

### استخدام البحث الذكي في Model جديد:

```php
use App\Http\Controllers\Admin\SponsorshipController;

$result = YourModel::where(function($query) use ($searchTerm) {
    // استخدام البحث الذكي
    app(SponsorshipController::class)->addSmartSearch(
        $query, 
        'your_column', 
        $searchTerm, 
        false // أو true للبحث الدقيق
    );
})->get();
```

---

## 📝 ملاحظات مهمة

### ✅ يجب استخدام البحث الذكي في:
- البحث عن الأسماء (أشخاص، مؤسسات، بنوك)
- البحث عن العناوين والمواقع
- البحث عن أي نص عربي

### ❌ لا تستخدم البحث الذكي في:
- الأرقام (رقم الهوية، رقم الملف)
- التواريخ
- البيانات الرقمية

### 💡 نصائح:
1. استخدم `$exactMatch = false` للبحث العام
2. استخدم `$exactMatch = true` للتحقق من التطابق التام
3. ضع البحث داخل `where(function(){})` لتجنب التداخل مع شروط أخرى

---

## 🎓 الخلاصة

نظام البحث الذكي يوفر:
- ✅ مطابقة دقيقة بغض النظر عن طريقة الكتابة
- ✅ دعم كامل للنصوص العربية والمختلطة
- ✅ سهولة الاستخدام والتطوير
- ✅ أداء عالي وكفاءة
- ✅ قابلية التوسع والتخصيص

---

## 📞 للمزيد من المعلومات

راجع ملف: `test_smart_search.php` لأمثلة عملية شاملة
