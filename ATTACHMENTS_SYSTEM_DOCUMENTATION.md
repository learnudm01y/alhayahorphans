# نظام المرفقات في بوابة تحديث البيانات

## التاريخ: 16 ديسمبر 2025

## نظرة عامة

تم إضافة قسم متكامل للمرفقات في بوابة تحديث بيانات المستخدم، يتيح للمستخدمين رفع المستندات والوثائق المطلوبة بطرق متعددة.

---

## ✨ المميزات الرئيسية

### 1. أنواع المرفقات الديناميكية
- يتم جلب أنواع المرفقات من جدول `document_types`
- عرض أنواع مختلفة حسب كل جمعية
- إمكانية تفعيل/تعطيل أنواع معينة من المرفقات

### 2. طرق متعددة لرفع الملفات

#### أ. التقاط صورة بالكاميرا 📸
- فتح الكاميرا مباشرة من المتصفح
- التقاط صور فورية بجودة عالية
- معاينة الصورة قبل الحفظ

#### ب. تسجيل فيديو 🎥
- فتح كاميرا الفيديو مع الصوت
- تسجيل فيديوهات مباشرة
- مؤشر التسجيل أثناء العمل
- إمكانية إيقاف التسجيل في أي وقت

#### ج. اختيار من الملفات 📁
- رفع ملفات من جهاز المستخدم
- دعم الصور والفيديوهات
- إمكانية رفع ملفات متعددة

### 3. معاينة فورية
- معاينة الملفات قبل الرفع
- عرض مصغرات الصور والفيديوهات
- إمكانية حذف ملفات من المعاينة

### 4. إدارة المرفقات الموجودة
- عرض المرفقات المرفوعة سابقاً
- معاينة الملفات الموجودة
- إمكانية حذف المرفقات القديمة

---

## 📋 الموقع في البوابة

تم إضافة قسم المرفقات **مباشرة بعد قسم أفراد الأسرة** في صفحة تحديث البيانات.

الترتيب:
1. البيانات الأساسية
2. البيانات الشخصية
3. معلومات السكن
4. **أفراد الأسرة**
5. **📎 المرفقات والوثائق** ← (القسم الجديد)

---

## 🗂️ البنية التقنية

### الملفات المعدلة

#### 1. Controller: `ShowGeneralRegisrationController.php`

**المسار:** `app/Http/Controllers/Users/ShowGeneralRegisrationController.php`

**التعديلات:**

```php
// جلب أنواع المرفقات المفعلة
$documentTypes = \App\Models\DocumentType::where('basic_enabled', 1)
    ->orWhere('family_enabled', 1)
    ->get();

// جلب المرفقات الموجودة
$existingAttachments = \App\Models\Attachment::where('registration_id', $file_id)
    ->get()
    ->groupBy('document_type');
```

**معالجة رفع الملفات:**

```php
// معالجة المرفقات الجديدة
if ($request->hasFile('attachments')) {
    foreach ($request->file('attachments') as $docTypeId => $files) {
        // توليد اسم الملف مع البادئة
        $prefix = $documentType->pref ?? 'DOC';
        $filename = "{$file_id}_{$prefix}_{timestamp}_{random}.{$extension}";
        
        // حفظ الملف
        $path = $file->storeAs('attachments', $filename, 'public');
        
        // حفظ بيانات المرفق
        Attachment::create([...]);
    }
}
```

#### 2. View: `generalRegisrationIndex.blade.php`

**المسار:** `resources/views/user/dashboard/component/generalRegisrationIndex.blade.php`

**المكونات المضافة:**

##### أ. قسم HTML للمرفقات
```blade
<div class="card card-custom mb-4">
    <div class="card-body">
        <div class="category-title">
            <i class="bi bi-paperclip field-icon"></i>
            المرفقات والوثائق
        </div>
        
        {{-- عرض أنواع المرفقات --}}
        @foreach($documentTypes as $docType)
            {{-- حقول الرفع والمعاينة --}}
        @endforeach
    </div>
</div>
```

##### ب. JavaScript لإدارة الكاميرا والملفات

**الدوال الرئيسية:**

1. **`openCameraForPhoto(docTypeId)`**
   - فتح الكاميرا لالتقاط صور
   - طلب أذونات الكاميرا
   - عرض واجهة الكاميرا

2. **`openCameraForVideo(docTypeId)`**
   - فتح الكاميرا لتسجيل فيديو
   - طلب أذونات الكاميرا والميكروفون
   - عرض واجهة التسجيل

3. **`capturePhoto()`**
   - التقاط لقطة من الكاميرا
   - تحويلها إلى ملف JPEG
   - إضافتها للمعاينة

4. **`toggleRecording()`**
   - بدء/إيقاف تسجيل الفيديو
   - استخدام MediaRecorder API
   - حفظ الفيديو بصيغة WebM

5. **`previewFiles(input, docTypeId)`**
   - معاينة الملفات المحددة
   - عرض مصغرات للصور والفيديوهات
   - إمكانية حذف ملفات من المعاينة

6. **`deleteAttachment(attachmentId, button)`**
   - حذف مرفق موجود
   - تأكيد الحذف من المستخدم
   - تحديث العرض

---

## 🎨 واجهة المستخدم

### تصميم الأزرار

```
┌─────────────────────────────────────────────────┐
│  📸 التقاط صورة  │  🎥 تسجيل فيديو  │  📁 اختيار من الملفات  │
└─────────────────────────────────────────────────┘
```

**الألوان:**
- 🔵 أزرق: التقاط صورة
- 🔴 أحمر: تسجيل فيديو
- 🟢 أخضر: اختيار ملف

### نافذة الكاميرا

```
┌────────────────────────────┐
│  📷 التقاط صورة / تسجيل فيديو  │  ✖
├────────────────────────────┤
│                            │
│     [معاينة الكاميرا]       │
│                            │
├────────────────────────────┤
│   [التقاط]    [إلغاء]      │
└────────────────────────────┘
```

### معاينة الملفات

```
┌─────────────────────────────────────┐
│  👁️ معاينة الملفات الجديدة:         │
├─────────────────────────────────────┤
│  [صورة1] [فيديو1] [صورة2] [صورة3]  │
│    ✖        ✖        ✖        ✖     │
└─────────────────────────────────────┘
```

---

## 📊 نموذج البيانات

### جدول `document_types`

```sql
- id: معرف نوع المستند
- description: وصف المستند (مثل: "صورة الهوية")
- pref: البادئة (مثل: "ID", "CERT")
- basic_enabled: مفعل للبيانات الأساسية
- family_enabled: مفعل لأفراد الأسرة
- deceased_enabled: مفعل للمتوفين
```

### جدول `attachments`

```sql
- id: المعرف
- registration_id: رقم الملف
- document_type: نوع المستند (FK)
- stored_file_name: اسم الملف المخزن
- original_file_name: الاسم الأصلي
- file_path: مسار الملف
- file_size: حجم الملف
- mime_type: نوع الملف
- uploaded_by: المستخدم الذي رفع الملف
- upload_date: تاريخ الرفع
```

### تسمية الملفات

**الصيغة:**
```
{file_id}_{prefix}_{timestamp}_{random}.{extension}

مثال:
12345_ID_20251216143052_a8f9d2.jpg
```

**المكونات:**
- `file_id`: رقم الملف الداخلي
- `prefix`: البادئة من document_types
- `timestamp`: التاريخ والوقت (YmdHis)
- `random`: أحرف عشوائية (6 أحرف)
- `extension`: امتداد الملف

---

## 🔧 المتطلبات التقنية

### أذونات المتصفح المطلوبة

1. **الكاميرا (Camera)**
   ```javascript
   navigator.mediaDevices.getUserMedia({ video: true })
   ```

2. **الميكروفون (Microphone)**
   ```javascript
   navigator.mediaDevices.getUserMedia({ audio: true })
   ```

### المتصفحات المدعومة

✅ **مدعومة بالكامل:**
- Chrome 60+
- Firefox 55+
- Safari 11+
- Edge 79+

⚠️ **دعم جزئي:**
- Safari iOS 11+ (قد تحتاج إعدادات إضافية)
- Chrome Android (يعمل بشكل جيد)

---

## 📱 الاستجابة للأجهزة المحمولة

### الهواتف الذكية
- تفعيل الكاميرا الخلفية تلقائياً: `facingMode: 'environment'`
- أزرار كبيرة سهلة اللمس
- واجهة مبسطة ومناسبة للشاشات الصغيرة

### الأجهزة اللوحية
- استغلال المساحة بشكل أفضل
- عرض مصغرات أكبر

---

## 🔒 الأمان والتحقق

### التحقق من نوع الملف
```php
accept="image/*,video/*"
```

### التحقق من الحجم
```php
'file_size' => $file->getSize()
```

### التحقق من نوع MIME
```php
'mime_type' => $file->getMimeType()
```

### تسجيل العمليات
```php
Log::info('ATTACHMENT_UPLOADED', [
    'file_id' => $file_id,
    'doc_type' => $documentType->description,
    'filename' => $filename,
    'size' => $file->getSize()
]);
```

---

## 🧪 اختبارات موصى بها

### اختبار 1: التقاط صورة
1. الضغط على "التقاط صورة"
2. السماح بالوصول للكاميرا
3. التقاط الصورة
4. التحقق من ظهورها في المعاينة

### اختبار 2: تسجيل فيديو
1. الضغط على "تسجيل فيديو"
2. السماح بالوصول للكاميرا والميكروفون
3. بدء التسجيل
4. إيقاف التسجيل
5. التحقق من الحفظ

### اختبار 3: رفع من الملفات
1. الضغط على "اختيار من الملفات"
2. اختيار ملفات متعددة
3. التحقق من المعاينة
4. حفظ النموذج

### اختبار 4: حذف مرفق
1. عرض مرفق موجود
2. الضغط على زر الحذف
3. تأكيد الحذف
4. التحقق من الإزالة

---

## 📝 ملاحظات مهمة

⚠️ **الأذونات:** يجب على المستخدم السماح بالوصول للكاميرا/الميكروفون في المتصفح

⚠️ **HTTPS:** بعض المتصفحات تتطلب HTTPS لاستخدام الكاميرا

⚠️ **الحجم:** الفيديوهات قد تكون كبيرة الحجم، يُفضل تحديد حد أقصى

⚠️ **الصيغة:** الفيديوهات تُحفظ بصيغة WebM، قد تحتاج تحويل لاحقاً

✅ **التوافق:** تم اختبار النظام على متصفحات Chrome, Firefox, Safari

✅ **الأداء:** تم تحسين الكود لتجنب تحميل الذاكرة

---

## 🚀 التطويرات المستقبلية المقترحة

1. **ضغط الصور والفيديوهات** قبل الرفع لتوفير المساحة
2. **معاينة مباشرة للفيديو** قبل الحفظ
3. **تحرير الصور** (قص، تدوير، فلاتر)
4. **رفع الملفات بشكل غير متزامن** (AJAX)
5. **شريط تقدم** أثناء رفع الملفات الكبيرة
6. **التحقق من صحة المستندات** باستخدام OCR
7. **إشعارات** عند اكتمال الرفع
8. **معرض صور** لعرض جميع المرفقات

---

## 📞 الدعم

في حالة وجود مشاكل:
1. تحقق من سجلات Laravel: `storage/logs/laravel.log`
2. تحقق من console المتصفح للأخطاء
3. تأكد من أذونات الكاميرا/الميكروفون
4. تأكد من وجود أنواع مرفقات مفعلة في `document_types`

---

**تم التطوير بنجاح ✓**
