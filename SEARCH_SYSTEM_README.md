# نظام البحث الشامل في السجلات

## نظرة عامة

تم تطوير نظام بحث شامل وقوي يتيح البحث في ثلاث جداول رئيسية:
- **السجلات الرئيسية** (data)
- **أفراد الأسرة** (re_people) 
- **سجلات المتوفين** (dead_people)

## الميزات الرئيسية

### 🔍 البحث المتقدم
- **البحث النصي الشامل**: ابحث في جميع الحقول النصية
- **البحث المحدد**: بحث دقيق برقم الملف، رقم الهوية، رقم الجوال
- **المرشحات المتقدمة**: فلترة حسب القسم، المدينة، الحالة الصحية، إلخ
- **البحث بالتاريخ**: فلترة حسب تاريخ الميلاد أو الوفاة
- **الإكمال التلقائي**: اقتراحات ذكية أثناء الكتابة

### ⚡ الأداء والسرعة
- **فهارس قاعدة البيانات محسنة**: فهارس مفردة ومركبة
- **التخزين المؤقت الذكي**: حفظ النتائج لمدة 15 دقيقة
- **تحديد معدل الطلبات**: حماية من الإفراط في الاستخدام
- **البحث المتوازي**: بحث سريع في جميع الجداول

### 🎨 واجهة المستخدم
- **تصميم responsive**: يعمل على جميع الأجهزة
- **نتائج ملونة ومنظمة**: أيقونات وألوان مميزة لكل نوع
- **تصفح متقدم**: عرض النتائج مع إمكانية التنقل
- **إحصائيات فورية**: عرض أعداد السجلات المختلفة

## الملفات المُنشأة

### Controllers
- `SearchOnRecordsController.php` - المتحكم الرئيسي للبحث

### Services  
- `SearchService.php` - منطق البحث والمعالجة

### Middleware
- `SearchRateLimiter.php` - تحديد معدل طلبات البحث

### Views
- `searchOnRecords.blade.php` - واجهة البحث

### Configuration
- `config/search.php` - إعدادات النظام

### Commands
- `OptimizeSearchPerformance.php` - أمر تحسين الأداء

### Migrations
- `add_search_indexes_for_performance.php` - فهارس قاعدة البيانات

## طريقة الاستخدام

### الوصول للنظام
```
المسار: /admin/search-records
الصلاحية: admin، supervisor، data_entry
```

### أنواع البحث

#### 1. البحث الشامل (جميع الجداول)
- ابحث في جميع الجداول الثلاثة مرة واحدة
- يعرض حتى 20 نتيجة من كل جدول
- مثالي للبحث السريع

#### 2. البحث في السجلات الرئيسية
- بحث مفصل في جدول data
- عرض مُصفح مع إمكانية التنقل
- مرشحات متقدمة متعددة

#### 3. البحث في أفراد الأسرة  
- بحث في جدول re_people
- ربط تلقائي مع السجل الرئيسي
- عرض بيانات العمر والحالة الصحية

#### 4. البحث في المتوفين
- بحث في جدول dead_people  
- عرض بيانات الوالدين المتوفين
- معلومات أسباب الوفاة

### المرشحات المتاحة

#### مرشحات عامة
- النص العام (أسماء، أرقام، هواتف)
- رقم الملف
- رقم الهوية  
- رقم الجوال
- الجنس
- تاريخ الميلاد (من - إلى)

#### مرشحات متخصصة
- القسم
- المدينة
- حالة الطلب
- صلة القرابة
- الحالة الاجتماعية
- المؤهل العلمي
- الحالة الصحية
- حالة العمل

## إعدادات النظام

### في ملف `config/search.php`:

```php
// الحد الأقصى للنتائج في الصفحة
'max_per_page' => 100,

// تفعيل التخزين المؤقت
'enable_cache' => true,

// مدة التخزين المؤقت (دقائق)
'cache_duration' => 15,

// حد البحث للمستخدم الواحد في الدقيقة
'rate_limit_per_minute' => 60,
```

## API Endpoints

### البحث
```
POST /admin/search-records
Parameters: search_type, search_text, filters...
```

### الإحصائيات
```
GET /admin/search-records/stats
Returns: أعداد السجلات في كل جدول
```

### الاقتراحات
```
GET /admin/search-records/suggestions?q=النص
Returns: قائمة الاقتراحات
```

### مسح الذاكرة المؤقتة
```
POST /admin/search-records/clear-cache
```

## أوامر الصيانة

### تحسين الأداء
```bash
# تحليل الجداول
php artisan search:optimize --analyze

# مسح الذاكرة المؤقتة  
php artisan search:optimize --clear-cache

# إعادة بناء الفهارس
php artisan search:optimize --rebuild-indexes

# تحسين شامل
php artisan search:optimize --full
```

### تطبيق الفهارس
```bash
php artisan migrate
```

## الأمان والحماية

### تحديد معدل الطلبات
- 60 طلب بحث كل دقيقة للمستخدم الواحد
- تسجيل جميع عمليات البحث
- حماية من هجمات DDoS

### الصلاحيات
- المسؤولون: وصول كامل
- المشرفون: بحث ومشاهدة
- مدخلو البيانات: بحث محدود

### تشفير البيانات الحساسة
- تشفير سجلات البحث الحساسة
- إخفاء الحقول الحساسة للمستخدمين غير المخولين

## تحسين الأداء

### فهارس قاعدة البيانات

#### جدول data
```sql
-- فهارس مفردة
idx_data_first_name, idx_data_id_number, idx_file_id_number

-- فهارس مركبة  
idx_data_name_composite (data_first_name, data_father_name)
idx_data_file_status (file_id_number, data_request_status)

-- فهرس النص الكامل
idx_data_fulltext_names (جميع حقول الأسماء)
```

#### جدول re_people
```sql
idx_re_people_person_id, idx_re_people_registration_id
idx_re_people_registration_person (registration_id, person_id)
```

#### جدول dead_people  
```sql
idx_dead_people_father_id, idx_dead_people_mother_id
idx_dead_people_re_file_id
```

### نصائح الأداء
1. استخدم البحث المحدد عند الإمكان
2. فعّل التخزين المؤقت
3. شغّل أمر التحسين دورياً
4. راقب سجلات الاستعلامات البطيئة
5. استخدم LIMIT في الاستعلامات الكبيرة

## استكشاف الأخطاء

### مشاكل شائعة

#### البحث بطيء
```bash
# تحقق من الفهارس
php artisan search:optimize --analyze

# مسح الذاكرة المؤقتة
php artisan search:optimize --clear-cache
```

#### نفاد الذاكرة
- قلل من `max_per_page` في الإعدادات
- زد من `memory_limit` في PHP

#### خطأ في الاتصال
- تحقق من إعدادات قاعدة البيانات
- تأكد من صحة الفهارس

### السجلات (Logs)
```bash
# سجلات البحث
tail -f storage/logs/laravel.log | grep "Search"

# سجلات الأداء  
tail -f storage/logs/laravel.log | grep "Performance"
```

## التطوير المستقبلي

### ميزات مخططة
- [ ] تصدير النتائج إلى Excel/PDF
- [ ] البحث الصوتي (Phonetic Search)
- [ ] البحث الضبابي (Fuzzy Search)  
- [ ] إحصائيات تفصيلية
- [ ] تقارير الاستخدام
- [ ] البحث المحفوظ (Saved Searches)
- [ ] تنبيهات البحث

### API متقدم
- [ ] GraphQL endpoints
- [ ] WebSocket للبحث الفوري
- [ ] مصادقة API tokens
- [ ] Rate limiting متقدم

## المطورون

تم تطوير هذا النظام بواسطة فريق التطوير لضمان:
- الأداء العالي
- الأمان المتقدم  
- سهولة الاستخدام
- القابلية للتوسع

## الدعم الفني

للمساعدة والدعم الفني:
- راجع ملفات السجلات
- استخدم أوامر التشخيص
- تحقق من إعدادات النظام
- اتصل بفريق التطوير
