# تحسينات تصدير Excel الشامل

## التاريخ: 28 يناير 2026 (التحديث النهائي)

## التحديثات المنفذة

### 1. إزالة جميع القيم الرقمية (IDs) من الأعمدة ✅

تم إزالة جميع الأعمدة التي تعرض القيم الرقمية (IDs) والاحتفاظ فقط بالقيم النصية من العلاقات.

#### ورقة Data:
- **تمت إزالة:**
  - `data_section_id` → تم الاحتفاظ فقط بـ "القسم"
  - `data_relationship` → تم الاحتفاظ فقط بـ "صلة القرابة"
  - `data_marital_status` → تم الاحتفاظ فقط بـ "الحالة الاجتماعية"
  - `data_academic_qualification` → تم الاحتفاظ فقط بـ "المؤهل الأكاديمي"
  - `data_displacement_status` → تم الاحتفاظ فقط بـ "حالة النزوح"
  - `data_city` → تم الاحتفاظ فقط بـ "المدينة"
  - `data_province` → تم الاحتفاظ فقط بـ "المحافظة"
  - `data_health_status` → تم الاحتفاظ فقط بـ "الحالة الصحية"
  - `data_user_insert_data` → تم الاحتفاظ فقط بـ "المستخدم الذي أضاف البيانات"

- **تمت إضافة أعمدة نصية جديدة:**
  - "حالة التوظيف" (من `employmentStatusBreadwinner`)
  - "حالة السكن" (من `housingStatus`)
  - "نوع السكن الحالي" (من `currentHousingType`)
  - "حالة الطلب" (من `requestStatus`)

#### ورقة DeadPepole:
- **تمت إزالة:**
  - `father_death_reason` → تم الاحتفاظ فقط بـ "سبب وفاة الأب"
  - `mother_death_reason` → تم الاحتفاظ فقط بـ "سبب وفاة الأم"

#### ورقة RePeople:
- **تمت إزالة:**
  - `sponsorship_status` → تم الاحتفاظ فقط بـ "حالة الكفالة"
  - `person_health_status` → تم الاحتفاظ فقط بـ "الحالة الصحية"
  - `person_type_of_guarantee` → تم الاحتفاظ فقط بـ "نوع الكفالة"

---

### 2. إضافة البيانات البنكية من جدول guardian_bank_accounts ✅

تم إضافة 7 أعمدة جديدة لورقة Data تحتوي على البيانات البنكية:

1. **اسم البنك** - يتم جلبه من علاقة `bank` مع جدول `bank_names` (عمود `description`)
2. **IBAN USD** - رقم الحساب الدولي بالدولار
3. **IBAN Shekel** - رقم الحساب الدولي بالشيكل
4. **رقم الحساب الجاري** - رقم الحساب الجاري
5. **رقم هوية صاحب الحساب** - رقم هوية الشخص المالك للحساب
6. **اسم ولي الأمر** - اسم ولي الأمر
7. **رقم هاتف ولي الأمر** - رقم هاتف ولي الأمر

**ملاحظة:** يتم جلب هذه البيانات من جدول `guardian_bank_accounts` بناءً على `file_id_number` مع التأكد من عرض القيم النصية فقط (مثل اسم البنك من `bank_names.description` بدلاً من ID).

---

### 3. إضافة حقول portal_general_registration_field_values بذكاء ✅

**تم إضافة البيانات من جدول portal بطريقة ذكية:**
- يتم جلب **فقط الحقول التي لها قيم فعلية** (غير فارغة)
- يتم ربطها بناءً على **رقم الهوية** لكل شخص
- **لا يتم إضافة أعمدة فارغة** غير مستخدمة

**آلية العمل:**
1. البحث في جدول portal عن جميع field_keys التي لها قيم فعلية
2. إنشاء أعمدة فقط لهذه الحقول المستخدمة
3. جلب القيم لكل شخص بناءً على identity_number
4. عرض "-" للأشخاص الذين ليس لديهم قيم

**النتيجة:** 
- بيانات portal **موجودة ومعروضة** لمن لديه بيانات
- **لا توجد أعمدة فارغة** كثيرة
- ملف Excel **منظم وسريع**

**الجداول المرتبطة:**
- `Data` → يرتبط بـ portal عبر `data_id_number`
- `DeadPepole` → يرتبط بـ portal عبر `father_id` و `mother_id`
- `RePeople` → يرتبط بـ portal عبر `person_id`

---

## العلاقات المستخدمة في التصدير

### ورقة Data:
```php
'section'                      // القسم
'categoryOfRelation'           // صلة القرابة
'maritalStatus'                // الحالة الاجتماعية
'academicQualification'        // المؤهل الأكاديمي
'displacementStatus'           // حالة النزوح
'city'                         // المدينة
'province'                     // المحافظة
'healthStatus'                 // الحالة الصحية
'employmentStatusBreadwinner'  // حالة التوظيف
'housingStatus'                // حالة السكن
'currentHousingType'           // نوع السكن الحالي
'userInserted'                 // المستخدم
'requestStatus'                // حالة الطلب
'guardianBankAccount.bank'     // البيانات البنكية + اسم البنك
```

### ورقة DeadPepole:
```php
'fatherDeathReason'  // سبب وفاة الأب
'motherDeathReason'  // سبب وفاة الأم
```

### ورقة RePeople:
```php
'sponsorshipStatus'  // حالة الكفالة
'healthStatus'       // الحالة الصحية
'guaranteeType'      // نوع الكفالة
```

---

## الملف المعدل

- **الملف:** `app/Services/RecordsExportService.php`
- **التاريخ:** 28 يناير 2026 (التحديث النهائي)
- **الحالة:** ✅ تم التنفيذ بنجاح

---

## ملاحظات مهمة

1. ✅ جميع القيم الرقمية (IDs) تم إزالتها من الأعمدة
2. ✅ يتم عرض القيم النصية فقط من خلال العلاقات
3. ✅ تم إضافة البيانات البنكية بشكل كامل
4. ✅ اسم البنك يتم جلبه من العلاقة وليس ID
5. ✅ **حقول portal يتم جلبها بذكاء** - فقط الحقول المستخدمة (whereNotNull + where != '')
6. ✅ الربط بناءً على **رقم الهوية** (identity_number)
7. ✅ في حالة عدم وجود قيمة يتم عرض "-" بدلاً من null
8. ✅ ملف Excel **محسّن وسريع** بدون أعمدة فارغة كثيرة

---

## مثال على الحقول الديناميكية المضافة

**عدد الحقول من portal:** 32 حقل (فقط التي لها قيم فعلية)

**أمثلة على الحقول المضافة:**
- `field_school_name` - اسم المدرسة
- `field_school_address` - عنوان المدرسة
- `field_grade` - الصف الدراسي
- `field_student_level` - مستوى الطالب
- `field_orphan_ambition` - طموح اليتيم
- `field_psychological_state` - الحالة النفسية
- `field_behavioral_state` - الحالة السلوكية
- `field_religious_commitment` - الالتزام الديني
- `field_quran_memorization` - حفظ القرآن
- `field_sponsorship_impact` - أثر الكفالة
- `field_mother_status` - حالة الأم
- `field_living_mother_id` - رقم هوية الأم (إذا كانت على قيد الحياة)
- ...والمزيد (32 حقل إجمالاً)

**فقط للأشخاص الذين لديهم قيم في هذه الحقول**

---

## الاستخدام

للحصول على ملف Excel الشامل المحدث:

**الطريقة الأولى - عبر الواجهة:**
- افتح صفحة إدارة السجلات (Records Management)
- ابحث عن زر "تصدير Excel الشامل"
- اضغط على الزر لتنزيل الملف

**الطريقة الثانية - الرابط المباشر:**
```
GET /admin/records-management/export-all
```

**دليل الاستخدام التفصيلي:**
افتح ملف `public/excel-export-guide.html` في المتصفح للحصول على دليل شامل مع أمثلة

سيتم تنزيل ملف Excel يحتوي على 3 أوراق:
1. **Data** - بيانات الأفراد الرئيسيين (مع البيانات البنكية)
2. **DeadPepole** - بيانات المتوفين
3. **RePeople** - بيانات أفراد الأسرة
