<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Fill Gaps Configuration
    |--------------------------------------------------------------------------
    |
    | تحديد ما إذا كان يجب إعادة استخدام الأرقام المحذوفة (ملء الفجوات)
    | أو استخدام MAX + 1 دائماً
    |
    */

    'fill_gaps' => env('CODE_FILL_GAPS', true),

    /*
    |--------------------------------------------------------------------------
    | Gap Search Limit
    |--------------------------------------------------------------------------
    |
    | الحد الأقصى لعدد الأرقام التي يتم البحث فيها عن الفجوات
    | إذا كان عدد الأرقام أكبر من هذا الحد، سيتم استخدام MAX + 1 مباشرة
    | (لتحسين الأداء)
    |
    */

    'gap_search_limit' => env('CODE_GAP_SEARCH_LIMIT', 10000),

    /*
    |--------------------------------------------------------------------------
    | Reserved Code Timeout
    |--------------------------------------------------------------------------
    |
    | مدة انتهاء صلاحية الأكواد المحجوزة (بالدقائق)
    | الأكواد التي لم تُستخدم خلال هذه المدة سيتم حذفها
    |
    */

    'reserved_timeout' => env('CODE_RESERVED_TIMEOUT', 1),

    /*
    |--------------------------------------------------------------------------
    | Sync Interval
    |--------------------------------------------------------------------------
    |
    | فترة المزامنة بين جدول reserved_codes وجدول data (بالدقائق)
    |
    */

    'sync_interval' => env('CODE_SYNC_INTERVAL', 5),

];
