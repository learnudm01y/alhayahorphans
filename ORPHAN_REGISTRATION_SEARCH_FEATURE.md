# تحديث نموذج تسجيل الأيتام - ميزة البحث والتحقق

## التاريخ: 22 نوفمبر 2025

## نظرة عامة
تم تحديث نموذج تسجيل الأيتام بإضافة نظام بحث شامل يتحقق من وجود المستخدم في قواعد البيانات المختلفة قبل السماح بالتسجيل.

---

## الميزات المضافة

### 1. بوابة البحث الجديدة
- **الموقع**: في الصفحة الأولى (بوابة تعليمات الإدخال)
- **الوظيفة**: البحث عن المستخدم باستخدام:
  - رقم هوية المعيل
  - رقم هوية اليتيم
  - اسم المعيل
  - اسم اليتيم
  - رقم هوية المتوفى
  - اسم المتوفى

### 2. قواعد البيانات المشمولة في البحث
يتم البحث في الجداول التالية:

#### قاعدة بيانات `aso`
- `data` - جدول البيانات الأساسية للمعيلين
- `re_people` - جدول أفراد الأسرة والأيتام
- `dead_people` - جدول المتوفين (الأب/الأم)

#### قاعدة بيانات `civilregistry`
- `persons` - جدول السجل المدني

---

## سيناريوهات العمل

### السيناريو الأول: المستخدم موجود مسبقاً
**الحالة**: تم العثور على رقم الهوية في أي من جداول (data, re_people, dead_people)

**النتيجة**:
1. عرض رسالة تأكيد بوجود سجل مسبق
2. عرض تفاصيل المستخدم:
   - الاسم الكامل
   - رقم الهوية
   - رقم الملف
   - نوع السجل (معيل/يتيم/متوفى)
3. عرض نموذج تسجيل الدخول:
   - حقل رقم الهوية (مملوء تلقائياً)
   - حقل كلمة المرور (4 أرقام)
   - زر الدخول

### السيناريو الثاني: موجود في السجل المدني فقط
**الحالة**: تم العثور على رقم الهوية في جدول persons فقط

**النتيجة**:
1. عرض رسالة بوجود البيانات في السجل المدني
2. عرض تفاصيل الشخص:
   - الاسم الكامل
   - رقم الهوية
   - تاريخ الميلاد
   - الجنس
3. عرض زر "ملء الحقول تلقائياً والمتابعة"
4. عند الضغط:
   - الانتقال لبوابة البيانات الأساسية
   - ملء جميع الحقول تلقائياً من السجل المدني
   - السماح بإكمال عملية التسجيل

### السيناريو الثالث: غير موجود في أي مكان
**الحالة**: لم يتم العثور على رقم الهوية في أي جدول

**النتيجة**:
1. المحاولة الأولى: عرض رسالة "لم يتم العثور على بيانات - يرجى التأكد والمحاولة مرة أخرى"
2. المحاولة الثانية: عرض رسالة مع زر "المتابعة لإنشاء سجل جديد"
3. عند الضغط على الزر: الانتقال لنموذج التسجيل الجديد

---

## التعديلات على الملفات

### 1. ملف العرض (View)
**الملف**: `resources/views/user/generalRegistration/create.blade.php`

#### التعديلات:
- ✅ **تعليق** قسم تعليمات إدخال البيانات (مع الاحتفاظ به للاستخدام المستقبلي)
- ✅ **إضافة** بوابة البحث الجديدة
- ✅ **إضافة** JavaScript للبحث والتفاعل مع النتائج

#### الكود المضاف:
```html
<!-- بوابة البحث الجديدة -->
<div class="card shadow-sm border-0 mb-4" dir="rtl">
    <div class="card-header bg-gradient-primary text-dark fw-bold">
        <i class="fas fa-search me-2"></i>البحث عن سجل موجود
    </div>
    <div class="card-body">
        <!-- حقل البحث وزر البحث -->
        <!-- منطقة عرض النتائج -->
    </div>
</div>
```

### 2. ملف التحكم (Controller)
**الملف**: `app/Http/Controllers/Users/GeneralRegistrationController.php`

#### الوظائف المضافة:

##### أ. `searchAllTables(Request $request)`
**الغرض**: البحث الشامل في جميع قواعد البيانات

**البحث في**:
1. جدول `data` (قاعدة بيانات aso)
2. جدول `re_people` (قاعدة بيانات aso)
3. جدول `dead_people` (قاعدة بيانات aso)
4. جدول `persons` (قاعدة بيانات civilregistry)

**الإرجاع**:
```json
{
    "found": true/false,
    "has_account": true/false,
    "source": "data|re_people|dead_people|civil_registry",
    "data": {
        "id_number": "...",
        "full_name": "...",
        "type": "...",
        "file_id_number": "..."
    },
    "message": "..."
}
```

##### ب. `fillFromCivilRegistry(Request $request)`
**الغرض**: جلب البيانات من السجل المدني لملء الحقول

**المعاملات**: رقم الهوية

**الإرجاع**:
```json
{
    "success": true,
    "data": {
        "data_id_number": "...",
        "data_first_name": "...",
        "data_father_name": "...",
        "data_grand_father_name": "...",
        "data_family_name": "...",
        "data_birth_date": "...",
        "data_gender": 1/2
    }
}
```

##### ج. تحديث وظيفة `store()`
**التعديلات**:
- ✅ إضافة كود للتحقق من وجود رقم الهوية في جدول `persons`
- ✅ إضافة رقم هوية المعيل إلى جدول `persons` إذا لم يكن موجوداً
- ✅ إضافة أرقام هوية أفراد الأسرة إلى جدول `persons`
- ✅ إضافة أرقام هوية المتوفين (الأب/الأم) إلى جدول `persons`

**الكود المضاف**:
```php
// التحقق من وجود المعيل في persons
$existsInPersons = \App\Models\Persons::where('CI_ID_NUM', $guardianIdNumber)->exists();
if (!$existsInPersons) {
    \App\Models\Persons::create([
        'CI_ID_NUM' => $guardianIdNumber,
        'CI_FIRST_ARB' => $request->input('data_first_name'),
        // ... باقي الحقول
    ]);
}

// إضافة أفراد الأسرة
foreach ($familyMembers as $member) {
    // حفظ في re_people
    // ثم إضافة إلى persons إذا لم يكن موجوداً
}

// إضافة المتوفين
// ... كود إضافة الأب والأم إلى persons
```

### 3. ملف المسارات (Routes)
**الملف**: `routes/web.php`

#### المسارات المضافة:
```php
Route::post('/search-all-tables', [GeneralRegistrationController::class, 'searchAllTables'])
    ->name('search.all.tables');
    
Route::post('/fill-from-civil-registry', [GeneralRegistrationController::class, 'fillFromCivilRegistry'])
    ->name('fill.civil.registry');
```

---

## التدفق الكامل للنظام

```
بداية التسجيل
    ↓
[بوابة البحث]
    ↓
المستخدم يدخل رقم الهوية أو الاسم
    ↓
    ├─→ [موجود في data/re_people/dead_people]
    │       ↓
    │   عرض نموذج تسجيل الدخول
    │       ↓
    │   المستخدم يدخل كلمة المرور
    │       ↓
    │   تسجيل الدخول الناجح
    │
    ├─→ [موجود في persons فقط]
    │       ↓
    │   عرض خيار "ملء الحقول تلقائياً"
    │       ↓
    │   ملء الحقول من السجل المدني
    │       ↓
    │   المستخدم يكمل البيانات الناقصة
    │       ↓
    │   حفظ السجل + إضافة إلى persons
    │
    └─→ [غير موجود]
            ↓
        محاولة أولى: "يرجى التأكد"
            ↓
        محاولة ثانية: "المتابعة للتسجيل"
            ↓
        ملء النموذج يدوياً
            ↓
        حفظ السجل + إضافة إلى persons
```

---

## حالات الاستخدام الخاصة

### 1. حفظ البيانات في جدول persons
**متى يحدث**: عند إتمام عملية التسجيل بنجاح

**البيانات المحفوظة**:
- رقم الهوية للمعيل
- الاسم الكامل (الأول، الأب، الجد، العائلة)
- تاريخ الميلاد
- الجنس
- المدينة
- العنوان

**أفراد الأسرة والمتوفون**:
- يتم إضافة جميع أفراد الأسرة إلى جدول persons
- يتم إضافة الأب والأم المتوفين إلى جدول persons
- يتم تسجيل تاريخ الوفاة إن وجد

### 2. التعامل مع الأخطاء
- إذا فشل الحفظ في persons: يتم تسجيل تحذير في اللوج ولا يتم إيقاف عملية التسجيل
- جميع عمليات الإضافة إلى persons محاطة بـ try-catch

---

## ملاحظات مهمة

### 1. الأمان
- ✅ استخدام CSRF Token في جميع طلبات AJAX
- ✅ التحقق من صحة البيانات قبل الحفظ
- ✅ استخدام Prepared Statements عبر Eloquent

### 2. الأداء
- ✅ البحث يتم بشكل تسلسلي (يتوقف عند أول نتيجة)
- ✅ استخدام `first()` بدلاً من `get()` للحصول على نتيجة واحدة فقط

### 3. سهولة الاستخدام
- ✅ رسائل واضحة بالعربية للمستخدم
- ✅ ملء الحقول تلقائياً عند التوفر
- ✅ السماح بمحاولتين قبل السماح بالتسجيل الجديد

### 4. الصيانة
- ✅ الكود المعلق محفوظ بين `<!-- -->` لإمكانية استخدامه مستقبلاً
- ✅ تسجيل جميع العمليات في اللوج
- ✅ رسائل خطأ واضحة للمطورين

---

## اختبار النظام

### اختبارات مطلوبة:

1. **اختبار المستخدم الموجود**:
   - إدخال رقم هوية موجود في data
   - التحقق من ظهور نموذج تسجيل الدخول
   - محاولة تسجيل الدخول بكلمة مرور صحيحة

2. **اختبار السجل المدني**:
   - إدخال رقم هوية موجود في persons فقط
   - التحقق من جلب البيانات تلقائياً
   - إكمال التسجيل والتحقق من الحفظ

3. **اختبار المستخدم الجديد**:
   - إدخال رقم هوية غير موجود
   - المحاولة مرتين
   - إكمال التسجيل الجديد
   - التحقق من إضافة البيانات إلى persons

4. **اختبار أفراد الأسرة**:
   - تسجيل عائلة كاملة
   - التحقق من إضافة جميع أفراد الأسرة إلى persons

5. **اختبار المتوفين**:
   - تسجيل يتيم مع بيانات الأب/الأم المتوفين
   - التحقق من إضافة المتوفين إلى persons

---

## الخطوات التالية (اختياري)

1. إضافة إمكانية البحث بالاسم الجزئي (wildcard search)
2. إضافة خاصية الإكمال التلقائي أثناء الكتابة
3. إضافة تقارير للمستخدمين الجدد المضافين إلى persons
4. إضافة واجهة لعرض جميع أفراد الأسرة المرتبطين برقم ملف واحد

---

## الدعم والصيانة

### ملفات تحتاج للمراجعة:
- `app/Http/Controllers/Users/GeneralRegistrationController.php`
- `resources/views/user/generalRegistration/create.blade.php`
- `routes/web.php`

### قواعد البيانات المستخدمة:
- `aso.data`
- `aso.re_people`
- `aso.dead_people`
- `aso.persons`
- `civilregistry.persons`

---

## الخلاصة

تم تنفيذ نظام بحث شامل ومتكامل يحقق جميع المتطلبات:
- ✅ البحث في جميع الجداول المطلوبة
- ✅ عرض نموذج تسجيل الدخول للمستخدمين الموجودين
- ✅ ملء الحقول تلقائياً من السجل المدني
- ✅ السماح بالتسجيل الجديد بعد التحقق
- ✅ إضافة البيانات الجديدة إلى جدول persons
- ✅ تعليق الأجزاء غير المستخدمة مع الاحتفاظ بها

النظام جاهز للاختبار والاستخدام! 🎉
